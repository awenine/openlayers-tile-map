{
  "name": "openlayers-tile-map",
  "version": "1.0.0",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "update-tiles": "rm -rf ./public/tiles/* && ls -1 ./map_image | head -n 1 | xargs ./tiling_script.sh"
  },
  "devDependencies": {
    "vite": "^7.1.11"
  },
  "dependencies": {
    "ol": "latest"
  }
}
